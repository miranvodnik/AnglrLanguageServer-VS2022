<UserControl x:Class="AnglrLangExtension.AnglrStateTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnglrLangExtension"
             xmlns:src="clr-namespace:AnglrJsonRpcMethods;assembly=AnglrJsonRpcMethods"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:RhsNodeSetToStringConverter x:Key="RhsNodeSetToStringConverter" />
    </UserControl.Resources>
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Left">
                <Border Visibility="{Binding IsCoreDataVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <Label>Core Set</Label>
                        <ListView ItemsSource="{Binding Path=stateCoreDatas}" ScrollViewer.CanContentScroll="True">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition />
                                                <ColumnDefinition />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Production Nr." Margin="0,0,5,0"/>
                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Production Name" Margin="0,0,5,0"/>
                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Syntax Rule" Margin="0,0,5,0"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Production.ProductionNumber}" />
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Production.ProductionName}" />
                                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Production.RhsNodeSet,
                                            Converter={StaticResource RhsNodeSetToStringConverter},
                                            ConverterParameter=1}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
                <Border Visibility="{Binding IsClosureDataVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <Label>Closure Set</Label>
                        <ListView ItemsSource="{Binding Path=stateClosureDatas}" ScrollViewer.CanContentScroll="True">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition />
                                                <RowDefinition />
                                                <RowDefinition />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition />
                                                <ColumnDefinition />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Production Nr." Margin="0,0,5,0"/>
                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Production Name" Margin="0,0,5,0"/>
                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Syntax Rule" Margin="0,0,5,0"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ProductionNode.ProductionName}" />
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ProductionNode.ProductionName}" />
                                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ProductionNode.ProductionName}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
                <Border Visibility="{Binding IsShiftTransitionDataVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <Label>Shift Set</Label>
                        <ListView ItemsSource="{Binding Path=shiftTransitionDatas}">
                        </ListView>
                    </StackPanel>
                </Border>
                <Border Visibility="{Binding IsGotoTransitionDataVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <Label>Goto Set</Label>
                        <ListView ItemsSource="{Binding Path=gotoTransitionDatas}">
                        </ListView>
                    </StackPanel>
                </Border>
                <Border Visibility="{Binding IsReductionDataVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <Label>Reductions</Label>
                        <ListView ItemsSource="{Binding Path=reductionDatas}">
                        </ListView>
                    </StackPanel>
                </Border>
                <Border Visibility="{Binding IsSRConflictsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <Label>Shift/Reduce Conflicts</Label>
                        <ListView ItemsSource="{Binding Path=SRConflicts }">
                        </ListView>
                    </StackPanel>
                </Border>
                <Border Visibility="{Binding IsRRConflictsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Vertical">
                        <Label>Reduce/Reduce Conflicts</Label>
                        <ListView ItemsSource="{Binding Path=RRConflicts }">
                        </ListView>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
