<UserControl x:Class="AnglrLangExtension.AnglrDebuggerStackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnglrLangExtension"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="TerminalTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Terminal Symbol" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Name" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Name}" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Value" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Value}" />
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="NonTerminalTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="NonTerminal Symbol" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Name" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Name}" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Value" Margin="0, 0, 10, 0" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Value}" />
            </Grid>
        </DataTemplate>
        <local:AnglrDebuggerStackSelector
            x:Key="StackSelector"
            TerminalTemplate="{StaticResource TerminalTemplate}"
            NonTerminalTemplate="{StaticResource NonTerminalTemplate}" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
            <Label Content="Stack"/>
            <TextBlock Text="{Binding StackNr}" MinWidth="10"/>
            <RadioButton Content="Hide" />
        </StackPanel>
        <DataGrid
            ItemsSource="{Binding ParserStack}"
            AutoGenerateColumns="False"
            CanUserSortColumns="False"
            CanUserReorderColumns="False"
            Grid.Row="1"
            Grid.Column="0"
            RowDetailsTemplateSelector="{StaticResource StackSelector}" Margin="0,1,0,-1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridCheckBoxColumn Header="IsTerminal" Binding="{Binding IsTerminal}"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
